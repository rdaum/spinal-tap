[package]
name = "spinal-tap"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"

[dependencies]
compio = { version = "0.18", features = ["net", "runtime"] }
crossterm = "0.29"
ratatui = "0.30"
serde = { version = "1", features = ["derive"] }
toml = "0.9"
